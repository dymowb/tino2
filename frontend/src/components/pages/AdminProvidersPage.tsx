import React from 'react';
import { Box, Typography } from '@mui/material';

const AdminProvidersPage: React.FC = () => (
  <Box>
    <Typography variant="h4" fontWeight="bold" sx={{ mb: 3 }}>Provider Verification</Typography>
    <Typography color="text.secondary">Coming in Step 7...</Typography>
  </Box>
);

export default AdminProvidersPage;
